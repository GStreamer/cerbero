import os
from cerbero.config import Platform, Architecture, Distro, DistroVersion

target_platform = Platform.ANDROID
target_distro = Distro.ANDROID
target_distro_version = DistroVersion.ANDROID_VANILLAICECREAM
target_arch = Architecture.RISCV64

#variants.override('nodebug')
# https://doc.rust-lang.org/nightly/rustc/platform-support/android.html
variants.override('norust')
