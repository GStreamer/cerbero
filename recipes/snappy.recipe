from cerbero import recipe

class Recipe(recipe.Recipe):
    name = 'snappy'
    version = '0.2'
    deps = ['glib', 'gstreamer', 'gst-plugins-base', 'clutter', 'clutter-gst', 'libXtst']

    def prepare(self):
        if self.config.target_platform == Platform.LINUX:
            self.configure_options += ' --enable-dbus'

